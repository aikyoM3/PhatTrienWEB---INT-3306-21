const package = require( '../models/packageModel' );
const asyncHandler = require( 'express-async-handler' );

const createPackage = asyncHandler( async ( req, res ) => {})
const viewPackageInformation = asyncHandler(async (req,res) => {})